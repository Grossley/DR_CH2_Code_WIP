if (active == true)
{
    alpha -= fadespeed
    growth += growspeed
    alpha
    color
    draw_rectangle(((x - growth) - 2), ((y - growth) - 2), ((x + growth) + 2), ((y + growth) + 2), true)
    1
    if (alpha <= 0)
        // WARNING: Popz'd an empty stack.
}
