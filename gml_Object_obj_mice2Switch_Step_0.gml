if (place_meeting(x, y, obj_mainchara) || place_meeting(x, y, obj_marker) || (1166 && place_meeting(x, y, obj_noelle_scared) && obj_noelle_scared.jumpy == 0))
{
    if (toggle == 0)
    {
    }
    toggle = 1
    if 285
    {
        with (obj_forcefield)
            // WARNING: Popz'd an empty stack.
    }
}
image_index = toggle
