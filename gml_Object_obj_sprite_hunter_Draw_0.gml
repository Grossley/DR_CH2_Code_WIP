if (obj_len > 0)
{
    var spriteName = spriteID
    var theString = (("Objects with " + spriteName) + ":")
    for (i = 0; i < obj_len; i++)
        theString += ("\n" + objectNames[i])
    0
    ((theString + 30) + 10)
}
