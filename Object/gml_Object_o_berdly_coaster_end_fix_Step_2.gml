with (all)
{
    if (visible == true)
    {
        if (sprite_index == spr_ralsei_walk_right)
            visible = false
        if (sprite_index == spr_susie_walk_right_dw)
            visible = false
        if (sprite_index == spr_krisr_dark)
            visible = false
    }
}
