if (destroyable == 1)
{
    if (other.big == 0)
        myspeed += 1
    if other.big
        myspeed = 4
    var _temp_local_var_1 = other
    event_user(0)
}
else if (destroyable == -1)
{
    var _temp_local_var_2 = other
    event_user(0)
}
