if (destroyable == 1)
{
    if (other.big == 0)
        myspeed += 1
    if other.big
        myspeed = 4
    with (stacktop)
        0
    flash = true
    if (((siner + swingdir) / 20) < sin((siner / 20)))
        swingdir *= -1
}
else if (destroyable == -1)
{
    with (stacktop)
        0
}
