if (con < 2)
    // WARNING: Popz'd an empty stack.
if (con >= 2)
{
    explodedraw += 1
    16777215
    (1.5 - (explodedraw / 10))
    draw_circle(x, y, ((sprite_width / 2) + (explodedraw * 2)), 0)
    1
}
