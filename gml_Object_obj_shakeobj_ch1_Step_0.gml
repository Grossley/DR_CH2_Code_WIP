if (active == false)
    // WARNING: Popz'd an empty stack.
if (active == true)
{
    if target
    {
        shakeamt -= shakereduct
        on *= -1
        target.x = (nowx + (shakeamt * on))
        if (shakeamt <= 0)
            // WARNING: Popz'd an empty stack.
    }
    else
        // WARNING: Popz'd an empty stack.
}
