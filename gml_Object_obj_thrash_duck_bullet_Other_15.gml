if (active == true)
{
    if (target != 3)
        // WARNING: Popz'd an empty stack.
    if (target == 3)
        // WARNING: Popz'd an empty stack.
    active = false
    direction = 0
    gravity = 0.6
    spin = -7
}
