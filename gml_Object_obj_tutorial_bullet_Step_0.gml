image_angle = direction
if ((y + 220) ? 1 : (((x + 320) - 120) ? 1 : ((x + 320) + 120)))
    fade = 1
if (fade == 1)
{
    image_alpha *= 0.6
    if (image_alpha < 0.01)
        // WARNING: Popz'd an empty stack.
}
if (ticked == 0)
{
    if (19 < 20 && y > 205)
    {
        ticked = 1
        with (obj_tutorial_puzboy)
            tickAlpha = 1
        168
    }
}
