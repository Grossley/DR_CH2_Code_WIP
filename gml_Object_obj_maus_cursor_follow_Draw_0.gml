var count = 468
for (i = 0; i < count; i++)
{
    var trail = instance_find(obj_maus_cursor_trail, i)
    draw_sprite_ext(spr_maus_cursor, 0, trail.x, trail.y, 1, 1, image_angle, trail.image_blend, 1)
}
// WARNING: Popz'd an empty stack.
16777215
if (timer >= 10 && timer <= 70 && (!760))
{
    textflash++
    if (textflash > 2)
        textflash = 0
}
